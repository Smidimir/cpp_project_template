include(config)

set(APP_TARGET_NAME ${APP_NAME}_app)
set(TEST_TARGET_NAME ${APP_NAME}_test)
set(COMMON_TARGET_NAME ${APP_NAME}_common)

set(COMMON_INTERFACE_TARGET_NAME ${APP_NAME}_common_interface)
